import 'package:flutter/material.dart';

class PayMethodsSection extends StatelessWidget {
  const PayMethodsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
